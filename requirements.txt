# Pinned to versions verified working under Windows Smart App Control.
# IMPORTANT: do NOT upgrade langchain/langgraph/langsmith past these pins.
# Newer langsmith (>=0.3) pulls the native `xxhash` DLL, which Smart App
# Control blocks on a fresh install (ImportError: DLL load failed _xxhash).

# Core dependencies
langgraph==0.2.62
langchain==0.3.14
langchain-community==0.3.14
langchain-core==0.3.63
langchain-groq==0.2.4
langsmith==0.2.11          # pinned: >=0.3 introduces the blocked xxhash dependency
groq==0.37.1
python-dotenv==1.2.2

# MCP Server
mcp==1.28.1
pydantic==2.13.4

# Data processing
pypdf==6.14.2
requests==2.32.5
arxiv==2.1.3

# UI
streamlit==1.59.2

# Evaluation / utilities
# numpy/pandas left unpinned so the host installs wheels matching its Python
# version (Streamlit Cloud defaults to 3.13; a hard numpy pin would fail to build).
numpy
pandas
tenacity==9.1.4
